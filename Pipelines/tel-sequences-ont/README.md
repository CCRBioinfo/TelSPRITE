This pipeline is designed to filter whole genome, long read sequencing data for reads with ectopic telomere sequence. To run the pipeline, perform the following steps:

-Clone the pipeline

-Ensure that your data is aligned, as a .bam file is needed as input to the pipeline

-Edit config.yaml to include desired parameters and paths to appropriate files

-Run the pipeline by submitting the script run_pipeline.sh